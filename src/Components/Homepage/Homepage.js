import React from "react";
import { useNavigate } from "react-router-dom";
import "./style.scss";

function Homepage() {
  const navigate = useNavigate();

  return <div class="Homepage"></div>;
}

export default Homepage;
